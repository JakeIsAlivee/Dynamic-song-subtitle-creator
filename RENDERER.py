import time

while True:
    try:
        filetorenderDIR = filetorenderDIR
        break
    except NameError:
        print('Sorry, you can run this ONLY from MAIN script.')
        time.sleep(999999999)
        continue



print(filetorenderDIR)